import React from 'react'
// import Card from './Card'

const CardGrid = ({ data }) => {
    const cards = data
    return (
            <div>
               
            </div>
    )
}

export default CardGrid
